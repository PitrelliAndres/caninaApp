{
  "name": "parkdog-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "docker:up": "docker-compose up -d",
    "docker:down": "docker-compose down",
    "docker:build": "docker-compose build --no-cache && docker-compose up -d"
  }
}